# 三国塔防 · 设计文档

> 开发规则见 `CLAUDE.md`：需求/设计先落本文档，再开发；下方表格随开发持续更新。
> 状态图例：⬜ 待办 / 🚧 进行中 / ✅ 完成 / ❌ 失败

## 子文档索引
| 文档 | 内容 | 对应需求 |
|------|------|----------|
| `docs/art-system.md` | 美术系统：形象/动画/战斗特效 | R6 |
| `docs/hero-asset-prompts.md` | 八将 AI 生图提示词（V2Fun.art · 路线 A） | R6 |
| `docs/gacha-system.md` | 招贤抽卡系统 + 数据配置化 + 武将/羁绊池 + 图鉴 | R8（重定义） |
| `docs/progression-system.md` | 武将养成：升级 + 专属碎片 + 被动技能 + 养成界面 | R9 |
| `docs/deck-levels.md` | 冲关阵容构筑（8 选 6）+ 关卡扩充 + 新敌人（盾兵/军医）| R10 / R11 |
| `docs/expansion-v2.md` | 内容扩充：+10 蜀将 + 新羁绊 + +10 关卡 + 图鉴详细页 | R13 / R14 / R15 |
| `docs/audio-system.md` | 音效与打击感：Web Audio 程序化音效 + BGM + 静音开关 | R12 |
| `docs/bosses-and-unlock.md` | 名将 Boss（能力引擎）+ 武将解锁/碎片招募 + 起始武将 + 弹性阵容 | R16 / R17 |
| `docs/ui-scenes.md` | UI 场景状态机 + 真·主界面（重度程序化背景）+ 弹窗全面场景化 | R19 |
| `docs/experience-r25.md` | 体验改版：武将府合并、选将详情、关卡差异化、美术升级 | R25 |
| `docs/ui-art-asset-brief.md` | UI/UE 美术资产包需求、规格与生成提示词 | R37 |
| `素材/DESIGN.md` | 图片素材规格与生成记录 | R26 |

> 文档拆分说明：R6、R8 设计内容较多，已按「文档拆分」规则拆至 `docs/` 子文档；本文档保留总览与全局进度表。

---

## 一、产品概述

- **类型**：网页塔防（HTML5 Canvas + 原生 JS，零构建依赖，双击 `index.html` 可玩）。
- **题材**：三国。武将作为防御塔，敌军为来犯部队，关卡对应经典战役。
- **核心循环**：摆武将塔 → 挡住波次敌军 → 击杀获军粮 → 造塔/升级/放计谋 → 守住全部波次胜利，城池血量归零失败。

## 二、文件结构

| 文件 | 职责 |
|------|------|
| `index.html` | 页面骨架、HUD、商店栏、技能栏、结算遮罩 |
| `style.css` | 全部样式 |
| `game.js` | 全部游戏逻辑（配置、状态、输入、更新、渲染） |

---

## 三、需求与设计

### 已实现需求

**R1 核心玩法 MVP**：固定行军路径、城池、军粮经济、波次系统、胜负判定、造塔/升级交互。

**R2 武将羁绊**：塔重构为武将（刘备/关羽/张飞/诸葛亮）+ 兵种原型（弓 archer / 枪 spear / 谋士 strategist）+ 阵营标签。羁绊「桃园结义」：刘关张同时在场，三人伤害 +25%。UI 显示激活羁绊 + 武将头顶绿色光环。

**R3 计谋主动技能**：HUD 技能栏含火计（定点范围爆发伤害）、空城计（全场减速）。CD 冷却机制 + UI 倒计时，键盘 1/2 触发，Esc 取消定点。

**R4 扩充内容**：新增枪兵原型（张飞，近程高伤瞬时命中）、攻城车 boss（高血量、撞城重伤）。关卡数据化：虎牢关 → 官渡 → 赤壁，各有独立地图/波次/起始经济，通关流转 + 最终胜利结算。

**R5 数值平衡**：用页内无头模拟器跑全关，修正「经济过剩 + 难度曲线反转」。砍击杀奖励约 40%，强化枪兵使集火可克 boss，缓和虎牢关开局。目标曲线：虎牢关易 → 官渡稳 → 赤壁险胜。

**R6 美术系统**（详见 `docs/art-system.md`）：为武将与小兵设置 Q 版头像形象（王者荣耀风格），程序化占位图 + 可替换为图片资源的架构；加入待机/攻击/受击/死亡动画与战斗特效（粒子、技能大招）。保持零依赖。

### 规划中需求（路线图）

> 优先级顺序：R7 → R8 → R9 → R10 → R11 → …。R7~R17 已完成。候选见下方「候选（未排期）」。

**R7 游戏框架 / QoL**（已完成）：主菜单+关卡选择、存档、暂停/加速、卖塔。

**R8 招贤抽卡 roguelike（重定义，详见 `docs/gacha-system.md`）**：将固定商店塔防改造为招贤抽卡玩法。
- 已确认决策：单局重置养成 / 招贤 3 选 1 / 两层花费（递增刷新费 + 按星级招募费）/ 武将唯一部署 / 重复抽到则升级、满级出池 / 武将池扩到约 8 / 数据配置化(`data.js`) / 图鉴页面。
- 子任务 R8-1~R8-7 见进度表。

**R9 武将养成系统（已完成，详见 `docs/progression-system.md`）**：新增第二条成长线「升级」（区别于战斗内「升星」）。
- 专属武将碎片（通关掉落）→ 永久升级（1→5）提升进关基础属性 → Lv3 解锁每将唯一被动技能。
- 含：存档扩展、养成界面、被动框架（onHit/onKill/aura/modifier）、通关碎片奖励。
- 子任务 R9-1~R9-7 见进度表。

**R10 冲关阵容构筑（已完成，详见 `docs/deck-levels.md`）**：进关前从武将池选 6 将组队（8 选 6），招贤只从所选阵容刷；选将界面 + 阵容存档。子任务 R10-1~R10-3。

**R11 关卡扩充 + 新敌人（已完成，详见 `docs/deck-levels.md`）**：新增 3 关（长坂坡/定军山/夷陵，总数 3→6）+ 2 新敌人（盾兵·护甲减伤 / 军医·治疗光环）。子任务 R11-1~R11-3。

**R12 音效与打击感（已完成，详见 `docs/audio-system.md`）**：Web Audio 程序化音效 + 程序化 BGM + 静音开关，零依赖可替换。

**R13 武将扩充（已完成，详见 `docs/expansion-v2.md`）**：新增 10 个蜀国武将（池 8→18）+ 4 条新羁绊；被动复用现有派发框架并扩展 armorBreak/execute/goldBonus/aura 多维/growth 机制。

**R14 关卡扩充（已完成，详见 `docs/expansion-v2.md`）**：新增 10 关（博望坡→剑阁，关卡 6→16），延续蜀汉战役线，碎片产出覆盖全部新武将。

**R15 图鉴武将详细页（已完成，详见 `docs/expansion-v2.md`）**：图鉴列表点击进入详细页，展示完整属性/养成/被动/所属羁绊。

**候选（未排期）**：通关三选一增益、阵营扩充（魏/吴）、无尽模式。

**R16 名将 Boss（已完成，详见 `docs/bosses-and-unlock.md`）**：8 名将作为关底 Boss，数据驱动「能力引擎」（冲锋/召唤/光环/狂暴/自愈/抗控）+ 顶部血条 + 大形象。

**R17 武将解锁与碎片招募（已完成，详见 `docs/bosses-and-unlock.md`）**：新增 4 起始 1★ 武将，开局仅 5 将；通关概率掉落全武将碎片（低稀有更高、featured 提升）；碎片招募解锁锁定武将；弹性出战阵容 1~6。

**R20~R24 体验与玩法扩展（用户排期，逐个实现）**：
- **R20 主界面背景精细版（已完成，详见 `docs/ui-scenes.md`）**：在 R19 程序化关隘背景上提升精细度——星空、山脊轮廓光、飘移云带、飞雁、前景松树剪影、卷动金边军旗、火把余烬、城楼窗呼吸光。静态层离屏缓存，动态层逐帧。
- **R21 战中结算过场动画（规划中，详见 `docs/cutscene.md`）**：通关/失败从静态弹窗升级为过场动画。
- **R22 通关三选一增益（规划中，详见 `docs/boons.md`）**：Roguelite 元素，每关通关后三选一局内增益。
- **R23 魏吴阵营扩充（待细化规模）**：新增魏/吴武将阵营 + 羁绊。
- **R24 无尽模式（规划中，详见 `docs/endless.md`）**：持续波次 + 难度递增 + 记录。

**R25 可读性与差异化体验改版（已完成，详见 `docs/experience-r25.md`）**：针对试玩反馈优化“玩起来腻”和“信息割裂”问题。
- **R25-1 武将府合并**：把图鉴与养成合并为单一入口，武将详情页同时展示属性、碎片、升级/招募、被动、参与羁绊。
- **R25-2 选将详情增强**：入关选将卡展示兵种/定位标签；点击卡片查看详情，点击勾选切换出战。
- **R25-3 关卡差异化**：后续为关卡增加敌情提示与明确主题压力，降低同质化。
- **R25-4 人物形象升级**：后续优先提升 UI 头像/立绘观感，再逐步替换战斗精灵。

**R26 武将素材实装试点（赵云，进行中）**：先用 `素材/赵云/赵云立绘1.png`、`素材/赵云/赵云精灵表1.png`、`素材/赵云/赵云技能1.png` 打通真实素材链路。
- **需求描述**：赵云在武将府、选将详情、战斗塔精灵中优先使用真实图片素材；详情页额外展示赵云技能图标组，让素材投入能被玩家明确感知。
- **设计方案**：把三张素材同步到 `assets/heroes/zhaoyun/` 作为运行时资产；`sprites.js` 预加载立绘、精灵表、技能图，并对伪透明棋盘格做运行时透明化；`game.js` 在武将详情中展示技能图。当前三张 `*1.png` 为 RGB 伪透明 PNG，可用但不是最终理想格式，后续最好提供带 alpha 的 PNG-32。
- **涉及文件/模块**：`assets/heroes/zhaoyun/*`、`sprites.js`、`game.js`、`style.css`、`素材/DESIGN.md`。
- **验收标准**：赵云详情页显示新立绘与技能图；战斗中赵云塔使用 4×2 精灵表帧；伪透明背景不明显残留；语法检查与浏览器实测无 console 报错。
- **修正记录（2026-06-13）**：用户本地仍显示兜底头像，判断为旧脚本缓存或 `file://` 下 canvas 图片处理失败导致。补充脚本版本号强制刷新缓存；图片透明化处理失败时回退为原始图片路径，保证不会退回灰色兜底。
- **修正记录（2026-06-13 二次）**：用户反馈立绘/技能图仍显示棋盘格背景，且技能图是多技能图标组，详情只介绍一个被动技能不应整张展示。改为离线生成真正带 alpha 的运行资产：`portrait.png`、`sheet.png`、`passive-icon.png`；详情页只显示单个被动图标。战斗精灵表继续使用透明 `sheet.png`，并补验收检查。
- **修正记录（2026-06-13 三次）**：用户战斗截图显示赵云仍为程序化兜底形象。判断为透明 sheet 仍经过运行时 canvas 清理，在 `file://` 或缓存场景下可能加载失败回退。改为赵云 `stripBg:false`，透明 PNG 直接绘制；被动图标与技能说明合并到同一「被动技能」区。武将府新增排序/筛选控件：排序按星级、等级、拥有状态、兵种、姓名；筛选按全部/已拥有/未拥有/弓/枪/谋。

**R27 战场视觉与可读性优化（进行中）**：针对战斗画面“像棋盘”、赵云过小发糊、相邻武将名字/形象重叠的问题做首轮重绘。
- **需求描述**：战斗场景从调试网格升级为更生动的草地/土路/关隘战场；赵云真实精灵在战场中清晰可辨；相邻武将的名字和形象不互相遮挡。
- **设计方案**：替换 `drawGrid/drawPath/drawCastle` 为 `drawBattlefield`（草地底纹、远山雾色、土路描边、碎石/草丛/旗帜/关门）；赵云图片精灵战斗尺寸单独放大并开启平滑；武将名称按同列堆叠做 Y 向避让，名牌加半透明底提升可读性；塔精灵按 y 排序绘制减少相互覆盖。
- **涉及文件/模块**：`game.js` 战斗绘制、`sprites.js` 图片精灵绘制参数、`DESIGN.md`。
- **验收标准**：赵云部署后明显使用真实精灵表且不糊成小点；相邻两名武将名字不重叠；战场不再呈现完整棋盘格；语法检查和自动回归通过。
- **修正记录（2026-06-13）**：用户反馈右侧招贤栏出现时战场画布被竖向拉伸。改为 CSS 固定 `#game` 显示比例 `aspect-ratio: 960 / 600`，`#stage` 只按宽度等比缩放画布，商店侧栏不挤压画布比例。

**R28 UI/UE 重构首轮（进行中）**：针对“UI 太糙”的整体反馈，先重构战斗主界面的信息架构与视觉系统。
- **需求描述**：战斗界面需要从“按钮堆叠 + 临时侧栏”升级为可扫描、可操作、三国题材一致的作战界面。玩家应该一眼看到关卡、资源、城防、波次、主操作；右侧应是清晰的军令/招贤/羁绊/当前选择信息面板；画布不能因布局变形。
- **设计方案**：保留原 DOM 功能点，重写样式层：`#sceneBattle` 作为战斗壳；HUD 改成顶部战况栏，标题/资源/波次/控制按钮分组；`#stage` 改成战场+军令面板布局；右侧 `#shop` 改为“军令面板”，招贤按钮突出，候选卡更像武将军牌，羁绊与选择信息收纳成独立信息块；统一按钮、边框、阴影、间距和响应式折叠。
- **涉及文件/模块**：`index.html`（少量文案/结构类名，如有必要）、`style.css`（主力重构）、`scripts/verify-r28-ui.js`、`DESIGN.md`。
- **验收标准**：战斗界面有明确的顶部战况栏和右侧军令面板；资源/城防/波次/技能/暂停加速/招贤层级清楚；画布保持 960:600 比例；窄屏下侧栏折叠到下方且无重叠；浏览器进入战斗无 console 报错。
- **修正记录（2026-06-13）**：用户截图反馈当右侧招贤/武将列表内容较长时，战场区域被竖向拉伸。修正为战场画布独立等比容器，右侧军令台独立滚动，不允许侧栏高度反向撑高画布。

**R29 战前流程 UI/UE 统一（进行中）**：承接 R28，把选关、选将、武将府从旧式列表卡片统一到同一套“三国军府/战报”视觉系统。
- **需求描述**：主界面进入选关、选将、武将府后，界面不应像临时表单或调试列表。玩家需要清楚知道当前处于哪个流程、下一步做什么、哪些信息是战报/敌情/阵容/武将详情，并且各页面之间按钮、卡片、标题、滚动区域的视觉语言一致。
- **设计方案**：不重写 JS 流程，主要通过 `style.css` 与少量文案类名强化信息架构：统一 `.scene-card` 为军府面板；选关列表改成战役令牌式条目，章节标题更像战报分隔；选将页改成左侧军册网格 + 右侧将领详情，底部出战栏固定层级；武将府列表改成军籍册条目，排序/筛选工具变成工具条；详情页与选将详情共用更精致的将领档案视觉。
- **涉及文件/模块**：`style.css`、`index.html`（标题/辅助文案可微调）、`scripts/verify-r29-prebattle-ui.js`、`DESIGN.md`。
- **验收标准**：选关/选将/武将府拥有统一面板、标题、列表、按钮风格；选关条目可扫描章节/状态/Boss；选将卡片不会重叠并突出选中态；武将府工具条、列表、详情信息层级清楚；移动端仍单列且无文本溢出；现有 R25/R26/R27/R28 回归不破。

**R30 主界面与反馈 UI polish（进行中）**：补齐 UI/UE 重构的体验边角，让主界面、按钮、提示和战斗结果弹窗从“功能可用”升级为有明确反馈与层级的游戏界面。
- **需求描述**：主界面信息应更像游戏入口而不是文字叠在画布上；按钮需要有主次、悬停/按下反馈；战中提示不能只挤在 HUD 一角；胜负弹窗需要和战斗视觉一致，有标题、正文、奖励/下一步的清晰层级；窄屏下仍不能遮挡或溢出。
- **设计方案**：保留现有 DOM 与 `flash()` 逻辑，增加 `#speedHint` 的 toast 化样式；统一 `.btn` 的阴影、按下和焦点状态；主界面增加暗角与底部面板感，概览 chip 改为状态仪表；`.overlay` 和 `.overlay-card` 改为战报弹窗，加入半透明遮罩、标题边线、按钮区层级；移动端调整主界面标题与底部区域。
- **涉及文件/模块**：`style.css`、`index.html`（缓存版本）、`scripts/verify-r30-polish.js`、`DESIGN.md`。
- **验收标准**：主界面标题/概览/按钮更有层级；按钮有 hover/active/focus 反馈；`#speedHint` 作为 toast 不挤压 HUD；胜负/开始弹窗视觉与军府面板一致；移动端标题和弹窗不溢出；全量回归脚本通过。

**R31 战斗操作状态卡（进行中）**：继续收尾战斗侧栏的 UE，把“当前选择/待部署/撤将”从文字说明升级为可扫描的操作状态卡。
- **需求描述**：玩家在战斗中需要一眼看懂当前是否有待部署武将、是否选中了场上武将、下一步该点击空地/抽招贤/按 S 撤将，以及撤将返还、伤害射程、被动/羁绊等关键状态。现状虽然功能完整，但说明文字密集、层级弱。
- **设计方案**：保留快捷键与点击逻辑，重写 `updateSelInfo()` 输出为结构化 HTML：空状态、待部署状态、已选武将状态分别带小标题、主名、数值网格和操作提示；候选卡增加 `data-state`，可招募/可升级/军粮不足状态更明确；CSS 增加 `.sel-card`、`.sel-stats`、`.sel-action`、`.cand-card[data-state]` 等样式。
- **涉及文件/模块**：`game.js`、`style.css`、`index.html`（缓存版本）、`scripts/verify-r31-battle-state-ui.js`、`DESIGN.md`。
- **验收标准**：右侧选择信息不再是长段落；待部署和已选武将有明确状态标题；伤害/射程/返还/升级等数值成组展示；候选卡能区分招募、升级、军粮不足；全量回归脚本通过。

**R32 全局视觉一致性收尾（进行中）**：清理旧式 UI 遗留，让前面几轮改造后的视觉语言更统一。
- **需求描述**：经过 R28-R31 后，主流程已经升级，但仍有旧式硬边框、内联样式、重复 CSS、未统一小标签和默认滚动条。这些细节会让界面看起来像多轮补丁叠加，不够完整。
- **设计方案**：移除 `game.js` 中羁绊说明的内联样式，改为 `.bond-need-line`；删除重复的 `.overlay.cut-reveal`；统一 `tower-btn`、`train-row`、`train-up`、`level-intel`、`.tw-fac/.cx-fac`、`.hero-tags em` 等旧控件/标签的背景、边框和圆角；增加 WebKit 滚动条样式，让列表滚动区域不再使用浏览器默认外观。
- **涉及文件/模块**：`game.js`、`style.css`、`index.html`（缓存版本）、`scripts/verify-r32-global-polish.js`、`DESIGN.md`。
- **验收标准**：主要样式文件不再有重复的 overlay 动画规则；羁绊需求说明无内联样式；旧控件使用统一 rgba 面板边框；滚动区域样式与军府面板一致；全量回归脚本通过。

**R33 战斗 HUD 信息架构重排（进行中）**：把顶部 HUD 从“所有控件平铺”改为关卡、资源、技能、控制四组，提升战斗中扫视效率。
- **需求描述**：当前 HUD 虽然样式升级过，但资源、开战、技能、暂停/加速/菜单仍混在同一层，玩家不容易快速区分“战况信息”和“可操作控件”。窄屏时也容易形成杂乱换行。
- **设计方案**：保留所有现有按钮 id 与 JS 绑定，在 `index.html` 中增加 `.hud-left`、`.hud-resources`、`.hud-actions`、`.hud-controls` 分组；`style.css` 将 HUD 改为分组化布局：左侧关卡名+资源，中间出战/计谋，右侧暂停/速度/音效/菜单；移动端按组换行，技能按钮保持稳定宽度。
- **涉及文件/模块**：`index.html`、`style.css`、`scripts/verify-r33-hud-layout.js`、`DESIGN.md`。
- **验收标准**：HUD 明确分为关卡/资源/技能/控制四组；所有原 id 保留；技能和控制按钮在桌面不与资源混排；移动端分组换行无溢出；全量回归脚本通过。

**R34 状态色彩语义增强（进行中）**：在不引入花哨装饰的前提下，给关键 UI 状态建立可扫读的颜色语义。
- **需求描述**：当前界面整体已经统一，但大量信息仍以棕金为主，军粮、城池、波次、火计、空城计、可升级、军粮不足、通关/锁定等状态不够一眼区分。需要通过克制的状态色增强可读性。
- **设计方案**：增加 `--info`、`--skill`、`--warn` 状态色；HUD 资源项通过 `data-kind` 区分 gold/hp/wave；技能按钮通过 `data-skill` 区分 fire/fort；关卡 cleared/boss/locked、候选 deploy/upgrade/short、选将 full/on 等状态统一使用 good/info/danger/skill 语义色；保持低饱和、半透明边框，不破坏三国军府主视觉。
- **涉及文件/模块**：`index.html`、`style.css`、`scripts/verify-r34-state-colors.js`、`DESIGN.md`。
- **验收标准**：HUD 三个资源块拥有不同语义边线；火计/空城计技能可视觉区分；候选卡升级/不足状态更明确；关卡通关/锁定/Boss 状态更清楚；全量回归脚本通过。

**R35 UI 基础质量门禁（进行中）**：为 UI/UE 重构建立自动质量门禁，避免后续改动重新引入粗糙样式。
- **需求描述**：当前已完成多轮 UI 重构，但如果没有统一质量检查，后续很容易出现缓存版本不一致、CSS 括号错误、重复关键规则、负字距/视口字体、内联样式等问题。这些都会让界面观感不稳定。
- **设计方案**：新增 `scripts/verify-r35-ui-quality.js`：检查 `index.html` 中 CSS/JS 缓存版本一致且为当前 R35；CSS 花括号平衡；禁止 `font-size: *vw`、负 `letter-spacing`、`style=` 内联样式（保留程序化头像 `--hero-color` 这类必要动态样式）；关键 overlay/HUD 规则不重复；确认 R28-R34 验证脚本可作为全量质量门禁串联。
- **涉及文件/模块**：`scripts/verify-r35-ui-quality.js`、`index.html`（缓存版本）、`DESIGN.md`。
- **验收标准**：质量脚本能独立通过；全量回归包含 R35；后续 UI 改动若破坏缓存版本、CSS 基础结构或引入粗糙样式会失败。

**R36 战场画框比例锁定（进行中）**：修复右侧招贤馆内容较长时，战场显示区域仍可能被竖向拉伸的问题。
- **需求描述**：用户截图显示战场在宽屏+高侧栏场景下被拉高，土路和角色都随显示区域纵向变形。需要让战场始终保持 960:600 的显示比例，不受右侧军令台高度影响。
- **设计方案**：在 `#stage` 内给 `#game` 增加 `.battle-frame` 外框，由外框负责 960:600 比例、宽度和定位；canvas 绝对铺满外框。右侧 `#shop` 继续独立滚动，不能反向撑高战场画框。补充验证脚本检查外框、比例和 canvas 铺满策略。
- **涉及文件/模块**：`index.html`、`style.css`、`scripts/verify-r28-ui.js`、`scripts/verify-r35-ui-quality.js`、`DESIGN.md`。
- **验收标准**：桌面战斗布局中 `.battle-frame` 固定 960:600；`#game` 只填充外框，不参与侧栏高度拉伸；窄屏仍按宽度等比缩放；自动验证通过。

**R37 UI/UE 美术资产包规划（已完成，详见 `docs/ui-art-asset-brief.md`）**：把后续重构所需的武将、战场、Boss、士兵、技能、战斗效果、UI 装饰素材先统一成可生成的美术需求。
- **需求描述**：继续 UI/UE 重构前，需要明确应该向生图工具索取哪些素材，以及每类素材的尺寸、透明度、分格、命名、验收标准和提示词，避免后续素材风格不一、无法透明接入、精灵表串帧等问题。
- **设计方案**：新增独立子文档，覆盖风格圣经、透明 PNG 硬性要求、精灵表分格、素材优先级、武将三件套、5 类士兵、8 名 Boss、5 类战场背景、主动技能/战斗反馈特效、UI 装饰图集与资源图标；所有模板尽量写成可复制给生图工具的提示词。
- **涉及文件/模块**：`docs/ui-art-asset-brief.md`、`DESIGN.md`。
- **验收标准**：文档能直接指导生成 P0/P1/P2 素材包；每类素材都有规格和提示词；包含透明/分格/可读性验收清单；总设计文档有索引和进度记录。
- **修正记录（2026-06-13）**：用户反馈不想自行拼接变量。补充「P0 直接复制提示词」章节，按 P0-01~P0-14 生成顺序给出完整可复制提示词，覆盖风格圣经、赵云三件套、五类士兵、虎牢关战场、UI 装饰图集、资源图标、火计/空城计图标。

**R38 P0 UIUE 素材实装（进行中）**：把用户放入 `素材/UIUE` 的 14 个 P0 素材接入游戏。
- **需求描述**：P0 素材已经生成并放入 `素材/UIUE`，需要应用到当前游戏中：赵云使用新版立绘/精灵/技能图标；敌军五类使用新精灵表；虎牢关战场使用新背景；火计/空城计和 HUD 资源使用新图标；UI 装饰素材作为界面纹理/装饰来源。已确认这些图片多为 RGB 假透明，不能直接拿来叠在 Canvas/UI 上。
- **设计方案**：保留 `素材/UIUE` 作为源文件；离线生成 `assets/uiue/` 运行时资产，对角色/士兵/图标/UI 图集做棋盘格/白底去底并输出 RGBA PNG，战场背景按游戏画布裁切/缩放为 960×600；在 `sprites.js` 增加 UIUE 资产覆盖与预加载；在 `game.js` 的战场、技能图标、资源图标渲染处使用新素材，并保留程序化绘制兜底。
- **涉及文件/模块**：`素材/UIUE/*`、`assets/uiue/*`、`sprites.js`、`game.js`、`style.css`、`index.html`、`scripts/verify-r38-uiue-assets.js`、`DESIGN.md`。
- **验收标准**：运行时角色/士兵/图标/UI 图集资产为 RGBA 且边角透明；虎牢关背景为 960×600；赵云新版三件套路径生效；五类敌军精灵表路径生效；火计/空城计 HUD 按钮展示新图标；自动验证和语法检查通过；浏览器实测无明显白底/棋盘格残留。

**R39 战场路线融合与敌兵落地修正（进行中）**：修复试玩截图中路线像外加粗管、敌兵像漂浮的问题。
- **需求描述**：新虎牢关背景图自带地形和路面质感，但当前游戏仍在其上叠加旧的粗棕色路径，导致路线与战场割裂；敌兵按中心点绘制，脚底没有贴到路面，移动时像悬空漂浮。需要让路径与底图融合，并让敌兵以脚底为锚点行走。
- **设计方案**：当 UIUE 战场背景可用时，不再绘制旧 `drawPath()` 厚实路面，改用 `drawRouteGuide()` 绘制低透明度的行军暗线、内侧尘土和细边，作为玩法可读提示；保留程序化背景时继续使用旧路面。敌兵绘制改为脚底锚点：根据精灵高度把绘制中心上移，阴影贴在 `en.y` 路面位置，并根据移动时间加入轻微踏步/压缩效果，避免悬浮感。
- **涉及文件/模块**：`game.js`、`scripts/verify-r39-battlefield-integration.js`、`DESIGN.md`。
- **验收标准**：UIUE 背景下不再出现大块纯棕色覆盖路面；路线只是轻量引导并与底图路面融合；敌兵脚底阴影贴住路径，移动时有轻微踏步；自动验证通过。

**R40 虎牢关路线锚点与行走融合（二次修正，已完成）**：继续修复试玩截图中“路线和地图不是一体、士兵飘着走”的观感问题。
- **需求描述**：R39 弱化了路线覆盖，但虎牢关底图本身已有自然弯曲土路，游戏逻辑仍沿 16×10 格子硬折线移动，会让士兵走在底图非路面区域；小兵只有平移和轻微缩放，缺少脚步节奏，仍容易像贴图漂移。
- **设计方案**：为 UIUE 虎牢关增加专用像素级 `visualPath` 锚点，敌军移动、路线提示和城门受击提示使用这条贴合底图土路的路径；原 `cells` 仍作为关卡配置，同时把视觉路径附近格子合并到 `pathBlocked`，避免武将部署到自然路面上。`drawRouteGuide()` 改为更窄的尘土/足迹提示，并增加 `drawRouteMarkers()` 分段足迹，避免整条棕色管道感。敌兵按路径切线轻微前后摆动、上下踏步与脚底阴影压缩，强化“踩在路上”的感觉；UIUE 背景下不再叠加旧程序化城门。
- **涉及文件/模块**：`game.js`、`index.html`（缓存版本）、`scripts/verify-r40-path-anchors.js`、`DESIGN.md`。
- **验收标准**：虎牢关 UIUE 背景下敌军行进坐标贴合底图土路，不再沿棋盘硬折线跨草地；路线提示不是粗面状覆盖，只保留低透明尘土和足迹；敌军脚底阴影随步态变化且锚在路面；自动验证通过。

**R41 战报结算 UI 升级（已完成）**：把战中开始/胜利/失败弹窗从普通文字卡升级为结构化战报，补齐 UIUE 重构中的结果反馈层。
- **需求描述**：当前过场动画结束后，`#overlay` 只显示标题、一段正文和按钮。通关奖励、当前关卡、下一关、失败原因等关键信息混在一行文字里，层级弱，和已经升级后的战场、HUD、军令台不匹配。
- **设计方案**：保留 `#overlay` 与按钮事件，扩展弹窗结构为战报卡：增加 `ovBadge` 状态徽记、`ovSummary` 摘要区、`ovRewards` 奖励芯片区、`ovNext` 下一步提示区。`game.js` 的 `applyResultOverlay()` 支持富结构 payload；`win()` 将碎片掉落转换为奖励芯片，`levelClear/win/retry/intro` 使用不同状态样式；`style.css` 为战报卡补充军报标题、奖励 chip、失败/胜利状态色和移动端布局。
- **涉及文件/模块**：`index.html`、`game.js`、`style.css`、`scripts/verify-r41-result-report.js`、`DESIGN.md`。
- **验收标准**：胜利结算能分区展示关卡结果、碎片奖励、下一关/返回主菜单；失败结算明确显示城防失守和重整提示；开战提示仍可正常开始；原 `ovBtn/ovBtn2` 事件不破坏；自动验证和全量回归通过。

**R42 主界面战役司令台升级（已完成）**：把主界面从“标题叠背景 + 底部条”升级为可操作、可扫描的战役入口。
- **需求描述**：当前主界面背景已经较精细，但 UI 层仍像静态标题页，继续/开始、武将府、战役进度、武将解锁数分散在底部概览条里，信息密度和后续战斗/战报界面不一致。需要让首屏一打开就像进入游戏司令台，而不是落地页。
- **设计方案**：保留 `homeCanvas` 动态背景和 `homeStart/homeHeroes/homeContinue` 事件，重构 DOM 为 `.home-command`：左侧为品牌、当前战役、主行动按钮；右侧为战役档案面板，展示章节、当前关卡、解锁武将、主推武将/敌情摘要和快捷入口。`renderHomeOverview()` 改为输出结构化 `.home-report` 和 `.home-metrics`，CSS 增加左右布局、状态徽记、进度条、移动端单列兜底。
- **涉及文件/模块**：`index.html`、`game.js`、`style.css`、`scripts/verify-r42-home-command.js`、`DESIGN.md`。
- **验收标准**：主界面首屏有明确司令台布局；继续/开始和武将府仍可点击；进度、章节、当前关卡、武将解锁数以结构化信息显示；移动端不重叠；自动验证和全量回归通过。

**R43 关卡选择战役沙盘升级（已完成）**：把选关页从普通按钮列表升级为战役卷宗/沙盘卡片，承接新主界面的司令台体验。
- **需求描述**：当前关卡选择页仍是章节标题加一列按钮，信息只显示关卡名、Boss 和状态。玩家进入主界面后第二屏视觉落差明显，也无法在选关页直接扫到敌情、推荐应对和掉落提升，仍有表单/调试列表感。
- **设计方案**：保留 `renderMenuList()` 的章节分组和 `enterLevel(i)` 点击逻辑，把 `appendLevelBtn()` 输出从简单按钮升级为 `.campaign-card`：包含关卡编号、战场名、状态徽记、Boss 徽记、敌情标签、主要威胁/推荐应对摘要、掉落提升武将；章节标题改为 `.campaign-chapter`，`menuList` 改为响应式卡片网格。锁定/通关/Boss 状态沿用语义色，移动端单列。
- **涉及文件/模块**：`game.js`、`style.css`、`index.html`（缓存版本）、`scripts/verify-r43-campaign-map.js`、`DESIGN.md`。
- **验收标准**：选关页每关以战役卡片展示；可见状态、Boss、敌情标签、威胁/推荐、掉落提升；未解锁仍不可点击；通关和 Boss 状态可扫读；移动端不重叠；自动验证和全量回归通过。

**R44 战场路线与士兵行走融合修正（已完成）**：针对试玩截图中“士兵路线和战场地图割裂、士兵像鬼魂飘着走”的问题做第三轮战场体验修正。
- **需求描述**：虎牢关底图已有自然土路，当前截图仍能看到粗厚、直角、像额外覆盖层的路线；敌兵沿路移动时缺少脚底接触、方向和重量感，像整张贴图漂浮平移。需要让路线成为底图的一部分，让士兵看起来踩在地上。
- **设计方案**：UIUE 虎牢关背景下不再绘制任何连续粗色带或旧棋盘路面，只保留低透明分段尘线、稀疏脚印和局部地面阴影作为玩法提示；首关即使背景尚未加载，也使用贴合 UIUE 的自然路面预览，避免短暂显示旧厚路。敌兵绘制改为脚底接触模型：根据路径切线微倾斜、脚底锚点绘制、落脚瞬间压低阴影并生成小尘点，减少“中心点滑动”的漂浮感。同步提升缓存版本，避免用户继续看到旧脚本。
- **涉及文件/模块**：`game.js`、`sprites.js`、`index.html`、`scripts/verify-r44-battlefield-grounding.js`、`DESIGN.md`。
- **验收标准**：虎牢关战斗画面没有大块棕色矩形路线覆盖；路线提示只表现为淡尘/脚印并贴合自然土路；敌兵有朝向、脚底阴影和落脚尘土，不再像贴图悬浮；缓存版本升级；自动验证和语法检查通过。

**R45 选将出战军册升级（已完成）**：继续 UI/UE 重构，把入关前阵容选择从普通卡片网格升级为清晰的出征编成界面。
- **需求描述**：主界面和选关页已升级，但选将页仍像旧式卡片列表，战场情报、阵容数量、已选武将和右侧详情之间缺少“出征决策”的层级。玩家需要更快判断当前关卡、阵容是否满足、哪些武将已上阵、下一步能否出战。
- **设计方案**：保留现有 `deckGrid/deckDetail/deckCount/deckStart/deckBack` 和选择逻辑，重构 DOM 与样式：顶部改为 `.deck-command`，展示战场名、出征状态、已选/上限/掉落提升/敌情指标；`deckIntel` 放入战场军报条；增加 `deckSlots` 阵容槽位 strip，实时显示已选武将头像/空位；左侧军册增加标题与说明，卡片更像军籍令牌；右侧详情增加“将领档案”标题和选中/未选状态徽记；底部出战栏改成固定决策条，按钮和阵容状态更靠近。
- **涉及文件/模块**：`index.html`、`game.js`、`style.css`、`scripts/verify-r45-deck-command.js`、`DESIGN.md`。
- **验收标准**：选将页顶部展示当前战场、状态、已选数量、掉落提升和敌情摘要；阵容槽位能实时反映 1~6 名已选武将与空位；左侧军册和右侧将领档案层级清楚；保留原有点击卡片看详情、勾选加入/移出、至少 1 人出战、最多 6 人限制；移动端单列无重叠；自动验证和语法检查通过。

**R46 武将府军籍档案升级（已完成）**：继续 UI/UE 重构，把武将府从普通列表升级为可扫描的将府档案管理界面。
- **需求描述**：武将府承担查看素材、招募、升级、被动、羁绊等长期养成信息，但当前仍是标题、工具条、列表的旧式结构；玩家很难一眼看到武将总量、已拥有、可升级、可招募和当前筛选结果。详情页也缺少和“军籍档案”一致的页面头。
- **设计方案**：保留 `codexSort/codexFilter/codexHeroes/codexBonds/detailBody/detailBack` 和原逻辑，重构 DOM：列表页增加 `.codex-command` 顶部，展示武将府标题、说明和 `codexMetrics` 概览指标；排序/筛选纳入 `.codex-tools` 军籍工具条；武将列表外包 `.codex-archive`，列表项增加状态徽记、等级/碎片/兵种/定位信息；羁绊区升级为 `.bond-dossier` 卷宗样式；详情页增加 `.detail-command` 页面头与“返回军籍”操作，详情卡与当前通用档案视觉衔接。
- **涉及文件/模块**：`index.html`、`game.js`、`style.css`、`scripts/verify-r46-codex-command.js`、`DESIGN.md`。
- **验收标准**：武将府顶部显示总武将、已拥有、可升级、可招募、当前筛选数量；排序/筛选仍可用；武将列表项能区分已拥有/未招募、升级/满级/待碎片状态；羁绊区为卷宗样式并保留需求展示；详情页有档案页头且招募/升级动作不破；移动端不重叠；自动验证和语法检查通过。

**R47 战斗军报反馈层升级（进行中）**：继续 UI/UE 重构，把战斗中的波次、名将登场、Boss 血条从普通提示升级为战场军报反馈。
- **需求描述**：战斗界面虽然已有 HUD、战场素材和结算战报，但波次开始/结束、名将登场/授首仍主要依赖 toast 或简单飘字；Boss 顶部血条偏调试条，缺少名号、能力状态和威胁感。玩家需要在战斗中更直观地感知“当前发生了什么”和“名将状态”。
- **设计方案**：保留 `flash()` 作为轻提示，新增 `game.battleNotice` 与 `showBattleNotice()`，在 canvas 顶部中央绘制 `.军报横幅` 风格的临时战报，覆盖波次开战、整备、名将现身、名将授首；重绘 `drawBossBar()` 为战场军令样式，显示 Boss 名号、血量、血条、能力状态标签（冲锋/狂暴/自愈/减伤/抗控），并加入低透明底板和状态色；保持不遮挡 HUD，不改变战斗数值。
- **涉及文件/模块**：`game.js`、`scripts/verify-r47-battle-notice.js`、`DESIGN.md`。
- **验收标准**：开始每波和挡下每波都有战场军报横幅；Boss 登场和授首有更醒目的军报反馈；Boss 血条显示名号、血量和状态标签；原 toast 与飘字仍可工作；自动验证和语法检查通过。

**R48 QA 体验修复（已完成）**：针对试玩 QA 反馈修复移动端主界面、file:// 素材加载、选将底栏遮挡、主界面 CTA 混淆、武将府伪招募按钮与 `verify-r25` 回归脚本。
- **设计方案**：主界面改为左栏品牌+次级入口、右栏战役档案+主 CTA「继续」；移动端 `#sceneHome` 改纵向流式布局并限制背景高度；`sprites.js` 在 `file://` 或 canvas 读像素失败时跳过棋盘格剥离；选将 `.deck-foot` 改文档流定位避免遮挡军册；武将府未招募行改为碎片提示文案；`verify-r25` 仅检查存在的 override 字段。
- **涉及文件/模块**：`index.html`、`style.css`、`game.js`、`sprites.js`、`scripts/verify-r25.js`、`scripts/verify-r42-home-command.js`、`scripts/verify-r45-deck-command.js`、`DESIGN.md`。
- **验收标准**：移动端首屏可见「战役地图/武将府」与「继续」；`file://` 打开无 SecurityError；选将底栏不遮挡卡片；全量 verify 脚本通过。

**R49 路径贴合与造塔平台（已完成）**：试玩反馈：敌兵行走像爬行、武将可摆在土路上、行进路线仍是折线直角。
- **设计方案**：虎牢关移动路径改为 Catmull-Rom 样条平滑 + 等距重采样；土路格阻挡与平滑路径对齐；新增 `HULAO_BUILD_PADS` 与底图木塔对齐，仅可在平台造塔并吸附坐标；敌兵行走仅用精灵表前两帧，取消身体前倾旋转，按行进方向水平翻转。
- **涉及文件/模块**：`game.js`、`sprites.js`、`index.html`、`scripts/verify-r49-path-gameplay.js`、`DESIGN.md`。
- **验收标准**：敌兵沿弯道平滑前进且站姿正常；武将只能摆在平台不在土路；待部署时高亮可用平台；自动验证通过。

- **修正记录（2026-06-14）**：试玩反馈路径仍偏离底图土路、建造点不准。对照 `hulao_gate.png` 重采样土路中心线，重设 `HULAO_VISUAL_PATH` 与 `HULAO_BUILD_PADS`；平台标记改为常驻金色虚线环+旗标，待部署时高亮并显示「塔」字。

**R51 战场左侧界面修正（已完成）**：试玩反馈左侧战场画布外框发绿、土路上有灰色圆点与造塔标记混淆、部分造塔点压在土路上。
- **设计方案**：对照底图重校 11 个造塔平台（移除压在土路上的 `{126,198}` 等点）；UIUE 底图模式下不再绘制路径脚印圆点；空闲时造塔点仅显示小旗标，待部署时才显示金色虚线环；画布外框改为深棕并与战场列同宽拉伸。
- **涉及文件/模块**：`game.js`、`style.css`、`index.html`、`scripts/verify-r51-battle-left-ui.js`、`DESIGN.md`。
- **验收标准**：土路上无灰色干扰圆点；造塔旗标落在草地木塔平台；画布外框与舞台融合；自动验证通过。

**R55 关口逼近路径（已完成）**：过 5 号塔后先沿土路右下逼近关隘，近关口再折向城门。
把「战场常驻 + 弹窗叠加」的伪场景改造为真正的场景状态机。
- 用户决策：重度程序化背景（新画三国关隘图）/ 养成·图鉴等弹窗全部升级为独立全屏场景 / 逐阶段验收。
- 新增 `game.scene` + `switchScene()`，R19 时场景为 home / levelSelect / deck / battle / barracks / codex；R25 后图鉴与养成合并为武将府（codex），不再保留独立 barracks 主入口。战中开战提示·结算·重试仍用 `#overlay`。
- 主界面：程序化关隘背景（远山/城楼/军旗/火把 + 轻动画）+ 标题 + 主按钮 + 存档概览条（通关进度 / 武将解锁数 X/22 / 「继续」快捷入口）。
- 分三期：R19-1 场景骨架 → R19-2 主界面 → R19-3 养成/图鉴场景化。子任务见进度表。

**R18 体验优化（QoL，已完成）**：试玩反馈修复与章节化。
- **章节化关卡选择**：16 关按战役线分 5 章（每章末关为 Boss 关），关卡列表分区显示并可滚动，修复列表过长溢出导致底部按钮（图鉴/养成）点不到的问题。`data.js` 新增 `CHAPTERS`，`game.js renderMenuList` 分章渲染，`style.css` 加分区标题 + `.menu-list` 滚动。
- **招贤候选可重新激活**：候选卡此前仅在渲染瞬间军粮足够才绑定点击；军粮回升后仍点不动。改为始终可点（`selectCandidate` 自带军粮校验），并在 HUD 刷新时实时切换 `.cant` 灰显。
- **招贤刷新费增长放缓**：`TUNING.recruitInc` 12 → 6（25,31,37… 取代 25,37,49…）。
- **出售计入全部升级费**：`invested` 本就累计部署+升级花费，但只返还 50% 故体感「没算升级」。改为**全额返还**总投入（部署+所有升级），售价 = 累计军粮投入。

---

## 四、实现进度

| # | 功能 | 设计 | 实现状态 | 负责模块/文件 | 备注 |
|---|------|------|----------|---------------|------|
| R1 | 核心玩法 MVP | ✅ | ✅ 完成 | game.js / index.html / style.css | 路径、波次、经济、胜负、造塔升级 |
| R2 | 武将羁绊系统 | ✅ | ✅ 完成 | game.js (HEROES/BONDS/recomputeBonds) | 4 武将 + 3 原型 + 桃园结义 +25% |
| R3 | 计谋主动技能 | ✅ | ✅ 完成 | game.js (SKILLS/cast*) | 火计、空城计、CD、键盘 1/2 |
| R4 | 枪兵/攻城车/多关卡 | ✅ | ✅ 完成 | game.js (LEVELS/loadLevel/startLevel) | 虎牢关→官渡→赤壁，攻城车 boss |
| R5 | 数值平衡调优 | ✅ | ✅ 完成 | game.js (ENEMY_TYPES/ARCHETYPES/LEVELS) | 奖励-40%，枪兵强化，曲线成型 |
| R6-A | Sprite 抽象层（proc/image 双路径 + 资源清单 + 兜底） | ✅ | ✅ 完成 | sprites.js (drawSprite/SPRITE_ASSETS/preloadSprites) | 可替换性核心 |
| R6-B | 武将程序化形象（刘/关/张/诸葛） | ✅ | ✅ 完成 | sprites.js (ProceduralSprites) | 王者风 Q 版 |
| R6-C | 小兵/Boss 形象（步兵/骑兵/攻城车） | ✅ | ✅ 完成 | sprites.js (ProceduralSprites) | |
| R6-D | 动画系统（待机/攻击/行走/受击/死亡） | ✅ | ✅ 完成 | game.js + sprites.js | time 驱动，攻击挥动/呼吸浮动/受击白闪 |
| R6-E | 粒子与战斗特效 | ✅ | ✅ 完成 | sprites.js (particles) | 命中爆点/箭拖尾/火球/火计火焰环/空城迷雾 |
| R6-F | 接入与联调 | ✅ | ✅ 完成 | game.js (draw*) | 替换 drawTowers/Enemies/Projectiles，MCP 实测 |
| R7-1 | 主菜单 + 关卡选择 | ✅ | ✅ 完成 | index.html / game.js (showMenu/enterLevel) | 未解锁关卡锁定，已通关标记 |
| R7-2 | 存档（localStorage） | ✅ | ✅ 完成 | game.js (loadSave/saveProgress/unlockLevel) | 记录 maxUnlocked，重载持久 |
| R7-3 | 暂停 / 加速 | ✅ | ✅ 完成 | game.js (togglePause/toggleSpeed) + index.html | 暂停冻结，1x/2x（多步进），空格/F |
| R7-4 | 卖塔返粮 | ✅ | ✅ 完成 | game.js (trySell/invested) | 返还累计投入 60%，按 S |
| R8-1 | 数据配置化（data.js） | ✅ | ✅ 完成 | data.js → GameData（IIFE 包裹避免撞名） | 抽出 RARITY/ARCH/HEROES/BONDS/SKILLS/ENEMY/TUNING/LEVELS |
| R8-2 | 武将池扩到 8 + 形象 | ✅ | ✅ 完成 | data.js + sprites.js | 赵云/马超/黄忠/庞统 + 差异化 dmgMul/rangeMul/rateMul/splash |
| R8-3 | 羁绊扩充 | ✅ | ✅ 完成 | data.js (BONDS) + recomputeBonds | 五虎上将(伤害+攻速)、卧龙凤雏，effect 多维 |
| R8-4 | 招贤抽卡系统（替换商店） | ✅ | ✅ 完成 | game.js + index.html | 3选1/递增刷新费+按星招募费/唯一部署/重复升级/满级出池/撤将返50% |
| R8-5 | 图鉴页面 | ✅ | ✅ 完成 | index.html + game.js (openCodex) | 8武将+3羁绊，GameData 自动渲染 |
| R8-6 | 难度递增 + 经济平衡 | ✅ | ✅ 完成 | data.js (TUNING/ENEMY/LEVELS) | 模拟器调平两层花费，曲线成型 |
| R8-7 | 联调与文档更新 | ✅ | ✅ 完成 | 全部 | MCP 真人化全流程实测 |
| R9-1 | 存档扩展 + 碎片/升级模型 | ✅ | ✅ 完成 | game.js (loadSave/meta/rankUp) + data.js | shards/ranks 持久化，容错补默认 |
| R9-2 | 升级影响基础属性 | ✅ | ✅ 完成 | game.js (towerStats) | meta 升级 +8% 伤/+3% 射程/级，顺序 升级→升星→羁绊 |
| R9-3 | 被动技能框架 | ✅ | ✅ 完成 | game.js (resolveHit/damageBy/onEnemyKilled) | onHit/onKill/aura/modifier 派发 + ownerTw 归属 |
| R9-4 | 八将被动接入 | ✅ | ✅ 完成 | data.js (HERO.passive) + game.js | 暴击/眩晕/减速/光环/远距/溅射/击杀爆 + 视觉反馈 |
| R9-5 | 养成界面 | ✅ | ✅ 完成 | index.html (#train) + game.js (openTrain) | 主菜单入口，碎片升级，被动解锁高亮 |
| R9-6 | 通关碎片奖励 | ✅ | ✅ 完成 | data.js (shardRewards) + game.js (win) | 结算文案显示掉落，写入存档 |
| R9-7 | 联调 + 平衡 + 文档 | ✅ | ✅ 完成 | 全部 | MCP 数据/被动/UI/实战四类断言通过 |
| R10-1 | 阵容存档 + 招贤池过滤 | ✅ | ✅ 完成 | game.js (deck/normalizeDeck/recruitPool) | deck 字段持久化，招贤只刷阵容内 |
| R10-2 | 选将界面 + 进关流程 | ✅ | ✅ 完成 | index.html (#deck) + game.js (showDeckSelect) | 关卡→选将(8选6,上限锁)→出战 |
| R10-3 | 阵容联调测试 | ✅ | ✅ 完成 | 全部 | MCP 上限/门槛/过滤/存档断言通过 |
| R11-1 | 新敌人（盾兵/军医） | ✅ | ✅ 完成 | data.js + sprites.js + game.js | 护甲减伤(min1) + 军医治疗光环 tick |
| R11-2 | 3 新关卡 + 碎片分配 | ✅ | ✅ 完成 | data.js (LEVELS) | 长坂坡/定军山/夷陵，总数 3→6 |
| R11-3 | 平衡 + 测试 + 文档 | ✅ | ✅ 完成 | 全部 | 模拟器跑6关 + MCP 机制/视觉实测 |
| R13-1 | 10 武将数据 + 4 羁绊 | ✅ | ✅ 完成 | data.js (HEROES/BONDS) | 姜维/魏延/法正/马岱/关平/关兴/张苞/王平/廖化/黄月英 |
| R13-2 | 被动派发框架扩展 | ✅ | ✅ 完成 | game.js (resolveHit/onEnemyKilled/towerStats/recomputeBonds) | armorBreak/execute/goldBonus/aura 多维/growth |
| R13-3 | 10 武将程序化形象 | ✅ | ✅ 完成 | sprites.js (ProceduralSprites) | 复用 chibi 底座，颜色/头饰/武器差异化 |
| R14-1 | 10 新关卡数据 | ✅ | ✅ 完成 | data.js (LEVELS) | 博望坡→剑阁，关卡 6→16 |
| R14-2 | 碎片产出覆盖 + 平衡抽测 | ✅ | ✅ 完成 | data.js + 模拟器 | 10 新将碎片均有产出，抽测无死局 |
| R15-1 | 图鉴详细页 | ✅ | ✅ 完成 | index.html + style.css + game.js (openHeroDetail) | 属性/养成/被动/羁绊，列表点击进入 |
| R12-1 | 音效引擎 audio.js | ✅ | ✅ 完成 | audio.js (window.Sfx) | AudioContext 解锁/三层增益/程序化合成/节流复音/可替换钩子/静音持久化 |
| R12-2 | BGM 程序化循环 | ✅ | ✅ 完成 | audio.js (startMusic/scheduler) | 五声音阶古风动机 lookahead 调度，进战开/退战停 |
| R12-3 | 触发点接入 + 开关 UI | ✅ | ✅ 完成 | index.html + game.js | 开火/击杀/暴击/计谋/扣城/波次/胜负 等接入；🔊 静音按钮 |
| R16-1 | Boss 数据 + 能力引擎 | ✅ | ✅ 完成 | data.js (BOSSES) + game.js | 8 名将；charge/rally/aura/enrage/regen/resist；固定血+控制抗性+减伤 |
| R16-2 | Boss 形象 + 血条 | ✅ | ✅ 完成 | sprites.js + game.js | 8 boss 程序化形象+帅旗；顶部专属血条 + 名号；大形象 |
| R16-3 | Boss 关卡接入 | ✅ | ✅ 完成 | data.js (LEVELS) | 第 1/3/5/6/8/13/15/16 关末波接入对应 Boss |
| R17-1 | 起始武将 + 解锁存档 | ✅ | ✅ 完成 | data.js (HEROES/STARTERS) + game.js (meta.unlocked) | 4 新 1★ 武将；开局仅 5 将；unlocked 持久化 |
| R17-2 | 概率碎片掉落 + featured | ✅ | ✅ 完成 | data.js (featured/TUNING) + game.js (rollShardDrops) | 按稀有度权重掉落、featured 提升、Boss 额外掉落 |
| R17-3 | 碎片招募 + 弹性阵容 | ✅ | ✅ 完成 | game.js + style.css | 养成界面招募解锁；图鉴/选将锁定门控；阵容 1~6 |
| R18-1 | 章节化关卡选择 + 滚动 | ✅ | ✅ 完成 | data.js (CHAPTERS) + game.js (renderMenuList) + style.css | 16 关分 5 章（每章末 Boss），分区+滚动，底部按钮可达 |
| R18-2 | 招贤候选可重新激活 | ✅ | ✅ 完成 | game.js (renderCandidates/updateCandidateAfford) | 始终可点 + HUD 实时灰显，军粮回升后可选 |
| R18-3 | 招贤刷新费增长放缓 | ✅ | ✅ 完成 | data.js (TUNING.recruitInc 12→6) | 25,31,37… |
| R18-4 | 出售全额计入升级费 | ✅ | ✅ 完成 | game.js (sellRefund) | 返还 = 部署+所有升级累计投入（100%） |
| R19-0 | 设计文档（场景化/主界面） | ✅ | ✅ 完成 | docs/ui-scenes.md + DESIGN.md | 场景状态机、主界面、分期、验收标准 |
| R19-1 | 场景骨架 + 关卡选择/选将/战场迁移 | ✅ | ✅ 完成 | index.html + game.js + style.css | game.scene/switchScene；HUD+stage 打包 battle；功能零丢失 |
| R19-2 | 主界面 home（重度背景+概览条） | ✅ | ✅ 完成 | sprites.js + index.html + game.js + style.css | 程序化关隘背景+轻动画；标题/按钮；通关进度/解锁数/继续 |
| R19-3 | 养成/图鉴弹窗 → 独立场景 | ✅ | ✅ 完成 | index.html + game.js + style.css | train/codex 场景化；heroDetail 子视图；返回导航统一（sceneReturn） |
| R20 | 主界面背景精细版 | ✅ | ✅ 完成 | sprites.js | 星空/轮廓光/云带/飞雁/松树剪影/卷动金边军旗/火把余烬/窗呼吸光 |
| R21 | 战中结算过场动画 | ✅ | ✅ 完成 | sprites.js (drawCutscene) + game.js + style.css | 胜利金光放射/失败城破裂纹 → 揭示结算卡（.cut-reveal 入场）；可点击跳过 |
| R25-0 | 体验改版设计文档 | ✅ | ✅ 完成 | docs/experience-r25.md + DESIGN.md | 记录试玩痛点、武将府/选将详情/关卡差异化/美术升级方案 |
| R25-1 | 武将府合并图鉴与养成 | ✅ | ✅ 完成 | index.html / game.js / style.css | 单一入口；详情页承载属性、碎片、升级/招募、被动、羁绊 |
| R25-2 | 选将兵种/定位标签 + 详情查看 | ✅ | ✅ 完成 | index.html / game.js / style.css | 卡片主体看详情，勾选按钮切换出战 |
| R25-3 | 关卡敌情提示与差异化首批 | ✅ | ✅ 完成 | data.js / game.js / style.css | 敌情简报 + rules 轻量关卡规则；虎牢关保留无额外规则教学关 |
| R25-4 | 人物 UI 形象升级 | ✅ | ✅ 完成 | assets/ / sprites.js / game.js / style.css | 首批：5 名已有 portrait 优先、17 名无图头像兜底、加载后刷新 UI |
| R26-1 | 赵云真实素材接入试点 | ✅ | ✅ 完成 | assets/heroes/zhaoyun / sprites.js / game.js / style.css / index.html | 离线生成透明 PNG；详情被动图标+说明合并；赵云透明 sheet 直接绘制不再回退程序化；武将府支持排序/筛选 |
| R27-1 | 战场视觉与武将可读性优化 | ✅ | ✅ 完成 | game.js / sprites.js / style.css | 草地土路战场、赵云战斗尺寸提升、塔名牌避让、画布比例锁定 |
| R28-1 | 战斗主界面 UI/UE 重构 | ✅ | 🚧 进行中 | style.css / index.html | 顶部战况栏、右侧军令面板、按钮与信息层级统一 |
| R28-2 | 战场画布防竖向拉伸修正 | ✅ | ✅ 完成 | style.css / scripts/verify-r28-ui.js | 画布固定 960:600 显示比例；右侧面板独立滚动，不撑高战场 |
| R29-1 | 战前流程 UI/UE 统一 | ✅ | ✅ 完成 | style.css / scripts/verify-r29-prebattle-ui.js | 选关、选将、武将府统一军府面板和列表/详情视觉 |
| R30-1 | 主界面与反馈 UI polish | ✅ | ✅ 完成 | style.css / scripts/verify-r30-polish.js | 主界面层级、按钮反馈、toast 提示、战报弹窗统一 |
| R31-1 | 战斗操作状态卡 | ✅ | ✅ 完成 | game.js / style.css / scripts/verify-r31-battle-state-ui.js | 右侧当前选择、待部署、撤将、候选状态结构化 |
| R32-1 | 全局视觉一致性收尾 | ✅ | ✅ 完成 | game.js / style.css / scripts/verify-r32-global-polish.js | 清理内联样式、重复 CSS、旧控件与滚动条外观 |
| R33-1 | 战斗 HUD 信息架构重排 | ✅ | ✅ 完成 | index.html / style.css / scripts/verify-r33-hud-layout.js | 顶部战况栏分为关卡、资源、技能、控制四组 |
| R34-1 | 状态色彩语义增强 | ✅ | ✅ 完成 | index.html / style.css / scripts/verify-r34-state-colors.js | 资源、技能、候选、关卡状态色语义增强 |
| R35-1 | UI 基础质量门禁 | ⬜ | 🚧 进行中 | scripts/verify-r35-ui-quality.js / index.html | 缓存版本、CSS 结构、内联样式与粗糙样式防回退检查 |
| R36-1 | 战场画框比例锁定 | ⬜ | 🚧 进行中 | index.html / style.css / scripts/verify-r28-ui.js | 通过固定比例外框阻断右侧侧栏撑高战场 |
| R37-1 | UI/UE 美术资产包规划 | ✅ | ✅ 完成 | docs/ui-art-asset-brief.md / DESIGN.md | P0 直接复制提示词 + 武将、士兵、Boss、战场、技能、战斗效果、UI 装饰规格 |
| R38-1 | P0 UIUE 素材处理与接入 | ✅ | ✅ 完成 | assets/uiue / sprites.js / game.js / style.css | 14 个 P0 素材去底、裁切并应用到战斗/UI |
| R39-1 | 战场路线融合与敌兵落地修正 | ✅ | ✅ 完成 | game.js / scripts/verify-r39-battlefield-integration.js | UIUE 背景下弱化路线覆盖，敌兵按脚底锚点绘制 |
| R40-1 | 虎牢关路线锚点与行走融合 | ✅ | ✅ 完成 | game.js / index.html / scripts/verify-r40-path-anchors.js | UIUE 虎牢关专用视觉路径、足迹提示、敌兵步态落地 |
| R41-1 | 战报结算 UI 升级 | ✅ | ✅ 完成 | index.html / game.js / style.css / scripts/verify-r41-result-report.js | 结果弹窗结构化为战报、奖励芯片、下一步提示 |
| R42-1 | 主界面战役司令台升级 | ✅ | ✅ 完成 | index.html / game.js / style.css / scripts/verify-r42-home-command.js | 主界面司令台布局、战役档案、进度和快捷入口 |
| R43-1 | 关卡选择战役沙盘升级 | ✅ | ✅ 完成 | game.js / style.css / index.html / scripts/verify-r43-campaign-map.js | 关卡卡片化、敌情摘要、Boss/状态/掉落信息 |
| R44-1 | 战场路线与士兵行走融合修正 | ✅ | ✅ 完成 | game.js / sprites.js / index.html / scripts/verify-r44-battlefield-grounding.js | 取消 UIUE 背景厚路线覆盖，增加脚底接触/尘土/朝向反馈 |
| R45-1 | 选将出战军册升级 | ✅ | ✅ 完成 | index.html / game.js / style.css / scripts/verify-r45-deck-command.js | 出征状态、阵容槽位、军册/档案布局升级 |
| R46-1 | 武将府军籍档案升级 | ✅ | ✅ 完成 | index.html / game.js / style.css / scripts/verify-r46-codex-command.js | 武将府概览、军籍列表、羁绊卷宗、详情页头升级 |
| R47-1 | 战斗军报反馈层升级 | ✅ | 🚧 进行中 | game.js / scripts/verify-r47-battle-notice.js | 波次军报、名将登场/授首反馈、Boss 状态血条 |
| R48-1 | QA 体验修复 | ✅ | ✅ 完成 | index.html / style.css / game.js / sprites.js / scripts/verify-r25.js | 移动主界面、file:// 素材、选将底栏、CTA 与武将府提示 |
| R49-1 | 路径贴合与造塔平台 | ✅ | ✅ 完成 | game.js / sprites.js / scripts/verify-r49-path-gameplay.js | 样条路径、箭塔平台、敌兵行走修正 |
| R51-1 | 战场左侧界面修正 | ✅ | ✅ 完成 | game.js / style.css / scripts/verify-r51-battle-left-ui.js | 造塔点离路、去路径圆点、画布外框、旗标简化 |

## 五、测试进度

| # | 功能 | 测试方式 | 测试状态 | 结果 | 备注 |
|---|------|----------|----------|------|------|
| R1 | 核心玩法 MVP | MCP 浏览器实测 | ✅ 完成 | 通过 | 造塔扣费/索敌/击杀/波次流转/升级均正常，无 console 报错 |
| R2 | 武将羁绊 | MCP 实测 + 脚本断言 | ✅ 完成 | 通过 | 三将 bondMult=1.25，伤害正确加成，面板与光环显示 |
| R3 | 计谋技能 | 确定性脚本断言 | ✅ 完成 | 通过 | 火计范围 90 伤、空城计减速 4s、CD（12s/18s）正确 |
| R4 | 多关卡/boss | MCP 实测 + 脚本 | ✅ 完成 | 通过 | 通关流转正确，攻城车 520 血/撞城-3，最终结算「天下大势已定」 |
| R5 | 数值平衡 | 页内无头模拟器 | ✅ 完成 | 通过 | 难度曲线：虎牢关漏3 / 官渡漏0 / 赤壁险胜(8塔剩4血) |
| R6 | 美术系统 | MCP 实测 + 定格截图 | ✅ 完成 | 通过 | Art 加载无报错；4武将+3敌人形象互相可区分；火计火焰环/受击白闪/CD UI 正常；实时战斗4s零报错 |
| R6 | 可替换性（SPRITE_ASSETS） | 设计审查 | ✅ 完成 | 通过 | img=null 回退程序化；填图片路径即切换，无需改调用点 |
| R7-1/2 | 菜单/关卡选择/存档 | MCP 实测 + 重载验证 | ✅ 完成 | 通过 | 锁定态正确；通关解锁官渡并写入 localStorage；重载后持久 |
| R7-3 | 暂停 / 加速 | 脚本断言 | ✅ 完成 | 通过 | 暂停冻结 update、按钮文案切换；speed 1↔2x 正确 |
| R7-4 | 卖塔返粮 | 脚本断言 | ✅ 完成 | 通过 | invested 50→升级90→卖塔返54(60%)，塔移除、军粮+54 |
| R8-1 | 数据配置化 | MCP 回归 | ✅ 完成 | 通过 | game.js 正确读 GameData；进关造塔出战无破坏，无报错（修复 const 撞名） |
| R8-2 | 武将池/形象/差异化 | 脚本+截图 | ✅ 完成 | 通过 | 8将形象可区分；黄忠超远(range189)、马超溅射38、赵云高攻速等数值生效 |
| R8-3 | 羁绊扩充 | 脚本断言 | ✅ 完成 | 通过 | 五虎(dmg1.35/攻速1.15)、卧龙凤雏(dmg1.4) 正确激活，多维加成 |
| R8-4 | 招贤抽卡系统 | MCP 实测+脚本 | ✅ 完成 | 通过 | 招贤扣递增费出3候选/选新将付招募费部署/重复升级/唯一部署/满级出池/撤将返50% |
| R8-5 | 图鉴页面 | MCP 实测+截图 | ✅ 完成 | 通过 | 8武将+3羁绊自动渲染，菜单图鉴入口正常 |
| R8-6 | 难度+经济平衡 | 无头模拟器(4×3关) | ✅ 完成 | 通过 | 虎牢关3/4漏7.5 / 官渡4/4漏1 / 赤壁2/4漏13.5；军粮留存合理(217/327/373)；注AI未用技能，真人更易 |
| R8-7 | 全流程联调 | MCP 真人化实测 | ✅ 完成 | 通过 | 菜单→招贤→点卡选将→布防→出战→火计→通关解锁官渡，零报错 |
| R9-1/2 | 存档+升级数据模型 | 确定性脚本断言 | ✅ 完成 | 通过 | 默认 rank1/shard0；虎牢关掉关羽+3张飞+2；升 Lv3 花 12 碎解锁被动；Lv1→Lv5 伤害 +28%(取整)；满级 cost=null；持久化重载 |
| R9-3/4 | 被动框架+八将被动 | 确定性脚本断言 | ✅ 完成 | 通过 | 关羽暴击×2+飘字、张飞眩晕、马超减速(0.6)、赵云龙胆主+溅、黄忠远距(27>19)、诸葛onKill旁爆、庞统溅射70、刘备仁德友军1.12/自身1.0 |
| R9-5 | 养成界面 | MCP 实测+截图+点击 | ✅ 完成 | 通过 | 8将渲染头像/星/Lv/碎片/被动；按碎片正确禁用启用；点升级→rank+1/扣碎/「已解锁」高亮/重渲染/持久化 |
| R9-6 | 通关碎片奖励 | 脚本断言 | ✅ 完成 | 通过 | win() 按 shardRewards 发碎入存档，结算文案显示战利 |
| R9-7 | 实战联调 | MCP 实战快进 | ✅ 完成 | 通过 | 解锁关羽+诸葛被动布防快进，wave1 全清零漏怪，新命中链零 console 报错 |
| R10 | 阵容构筑(8选6) | MCP 实测+截图+脚本 | ✅ 完成 | 通过 | 选将界面8卡默认选6；移除→5出战禁用；加→6；超6拒绝；出战存档+running；招贤池排除非阵容(马超/赵云不出)，pool 全在 deck |
| R11-1 | 新敌人机制 | 确定性脚本断言 | ✅ 完成 | 通过 | 盾兵护甲：20→14、3→1(min)；军医范围内回血、范围外不回；6关名+sprite 注册正确 |
| R11-2/3 | 新关卡+平衡 | 无头模拟器+MCP 截图 | ✅ 完成 | 通过 | 6关固定强阵 8塔 0漏(无死局)；难度在经济 ramp（设计预期）；定军山盾兵/军医形象可区分、绿治疗光环，零报错 |
| R13-1 | 10武将+4羁绊数据 | 确定性脚本断言 | ✅ 完成 | 通过 | HEROES=18/BONDS=7；4新羁绊正确激活(关门虎子1.3/虎贲1.2+攻速1.1/武侯传人1.3/机关连弩1.25+1.1) |
| R13-2 | 被动派发框架扩展 | 确定性脚本断言 | ✅ 完成 | 通过 | aura多维(姜维攻速1.18/王平射程1.15并生效)、魏延破甲6→1、马岱追斩(15%斩、50%不斩)、法正击杀+6粮(delta16)、廖化随波成长(wave4 +20%=58) |
| R13-3 | 10武将程序化形象 | 离屏渲染断言 | ✅ 完成 | 通过 | 10新sprite 全部无异常、各绘~2.2k+ 像素；18将 sprite 注册无缺失；实战 draw 零报错 |
| R14-1/2 | 10新关卡+平衡 | 无头模拟器(7~16关) | ✅ 完成 | 通过 | 16关数据合法(shard/wave 引用全有效)；10新将碎片均有产出；强阵16塔跑10关全 0漏、无死局、波次正常终止 |
| R15-1 | 图鉴武将详细页 | MCP 实测+点击 | ✅ 完成 | 通过 | 图鉴18行可点；点姜维弹详细页(属性伤28/射115/攻0.95/溅46、Lv/碎片、被动锁态、所属羁绊)，图鉴隐藏；返回回图鉴 |
| R13/14/15 | 全流程联调 | MCP 真实流程 | ✅ 完成 | 通过 | 进新关(江陵)→选含新将阵容→出战→布新将塔→开波，120帧 update+draw 零报错，击杀计粮正常、无漏怪 |
| R12-1 | 音效引擎 | 脚本断言(自动化) | ✅ 完成 | 通过 | API 完整；AudioContext 解锁=running；16 音效合成全不抛错；复音上限 14；静音→不发声；偏好持久化 sgtd_audio；节流(ui 40ms 内第二次丢弃) |
| R12-2 | BGM 循环 | 脚本断言 | ✅ 完成 | 通过 | startMusic→musicOn、stopMusic→off；进战(confirmDeck)自动起、退战停 |
| R12-3 | 触发点+开关 UI | MCP 实战驱动 | ✅ 完成 | 通过 | 江陵实战 200帧 update+draw：BGM 持续、开火/击杀 voice 活跃、零 console 报错；🔊 按钮 toggle+持久化。注：音频输出无法经 MCP 捕获，以代码路径断言验证，听感待人工试听 |
| R16-1 | Boss 能力引擎 | 确定性脚本断言 | ✅ 完成 | 通过 | 冲锋(×2)/召唤(+3骑兵)/光环(近1.2 远1.0)/狂暴(减伤0.3 速1.6)/自愈(+14/s)/抗控(boss减速2s vs 常规4s)；减伤+护甲 100→67 |
| R16-2 | Boss 形象 + 血条 | 离屏渲染 + MCP 截图 | ✅ 完成 | 通过 | 8 boss 各绘~2.8k+ 像素无异常；实战截图顶部血条「⚔吕布·飞将 368/520」+ 帅旗 + 名号正确，零报错 |
| R16-3 | Boss 关卡接入 + 平衡 | 页内无头模拟器 | ✅ 完成 | 通过 | 8 boss 关均接入正确 id；强阵跑 8 关全 won、boss 授首、无 stuck；L1 仅 5 起始将(Lv1~3 无被动)亦可 0 漏通关含吕布 |
| R17-1 | 起始武将 + 解锁存档 | 确定性脚本断言 | ✅ 完成 | 通过 | 22 武将；新存档仅 5 起始将解锁；unlocked 持久化；4 新 sprite 各绘~2.2k+ 像素 |
| R17-2 | 概率碎片掉落 | 4000 次分布抽样 | ✅ 完成 | 通过 | featured(关羽) 占比 14.3% 明显领先同侪；3★聚合 5.5% 最低；逐将低稀有更易掉；draws L1=5→L16=14 递增 |
| R17-3 | 碎片招募 + 弹性阵容 + UI | 脚本断言 + DOM 校验 | ✅ 完成 | 通过 | 招募扣碎/解锁/置 Lv1/持久；养成 17 锁定将显招募键(碎不足禁用)；图鉴 17 锁定标注；选将仅 5 起始将、弹性 1~6(0 禁用)、featured 注明 |
| R18-1 | 章节化关卡选择 + 滚动 | MCP 实测 + 截图 + DOM 校验 | ✅ 完成 | 通过 | 5 章渲染、16 关、8 boss 标记(⚔)正确；每章末关均为 Boss 关；menu-list overflow auto 可滚(601<1081)；overlay-card 底 949≤视口 1073，养成/图鉴按钮可达 |
| R18-2 | 招贤候选可重新激活 | MCP 驱动断言 | ✅ 完成 | 通过 | 军粮 0 时 3 卡全 .cant；军粮回升+updateHUD 后全 .cant=false（实时）；点击此前禁用卡成功选将、候选清空 |
| R18-3 | 招贤刷新费增长放缓 | 脚本断言 | ✅ 完成 | 通过 | recruitInc 6；进程 25,31,37,43,49,55（替代 25,37,49…） |
| R18-4 | 出售全额计入升级费 | MCP 实战断言 | ✅ 完成 | 通过 | guansuo 部署 40+两次升级=投入 124；售价显示 124（全额）；卖出军粮 +124，含全部升级 |
| R19-1 | 场景骨架 + 链路迁移 | MCP 实测 + DOM 校验 | ✅ 完成 | 通过 | 落地 levels 场景（5 章/16 关/8 Boss 标记）；enter→deck→battle（canvas 265 采样在绘制，招贤扣 25 抽 3）→菜单回 levels；codex 22 行/train 弹窗仍可开；零游戏报错 |
| R19-2 | 主界面 + 背景 + 概览条 | MCP 实测 + canvas 导出 | ✅ 完成 | 通过 | 落地 home（背景 922 采样在绘制：暮色天/远山/城楼/帥旗/火把）；概览「战役进度 第二章 第6/16关 · 武将 6/22 · 继续：夷陵之战」；开始→levels/返回→home/继续→deck/养成·图鉴弹窗/菜单→home 全通；零报错 |
| R19-3 | 养成/图鉴场景化 | MCP 实测 + DOM 校验 | ✅ 完成 | 通过 | home/levels→图鉴(codex 场景 22 行)→点将→详情子视图(列表隐藏/detailBody 充实/仍在 codex)→返回列表→返回来源；养成(barracks 22 行)同链；sceneReturn 正确回 home/levels；codex 卡 560×607 入屏；零报错 |
| R20 | 主界面背景精细版 | MCP canvas 导出审阅 | ✅ 完成 | 通过 | 导出 home 背景 PNG 目检：星空/山脊轮廓光/云带/飞雁/前景松树/卷动金边帥旗/火把余烬/窗呼吸光均到位；node --check 通过；无新增报错（getImageData taint 为既有 file:// 立绘问题，与本期无关） |
| R21 | 战中结算过场动画 | MCP 实测 + 帧导出 | ✅ 完成 | 通过 | win() 后 cutscene 激活(win)、overlay 隐藏；过场结束自动揭示「大捷！/进军下一关/levelClear」带 .cut-reveal；导出 win(p0.72 金光放射+大捷)/lose(p0.62 裂纹+关隘失守)帧目检到位；点击可跳过；无新增报错 |
| R25-1 | 武将府合并图鉴与养成 | node --check + scripts/verify-r25.js；浏览器实测 | ✅ 完成 | 通过 | 首页仅保留「武将府」入口，无旧「养成/图鉴」入口；武将府 22 将可滚动查看；点刘备进入详情，属性、Lv/碎片、升级按钮、被动、参与羁绊均正常；零游戏 console 报错 |
| R25-2 | 选将兵种/定位标签 + 详情查看 | node --check + scripts/verify-r25.js；浏览器实测 | ✅ 完成 | 通过 | 选将卡展示兵种/定位/Lv/被动；点击周仓卡片仅切换右侧详情；点击圆形勾选后阵容 5/6→4/6，详情按钮变「加入阵容」；出战进入 battle 正常 |
| R25-3 | 关卡敌情提示与差异化首批 | node --check + scripts/verify-r25.js；浏览器实测 | ✅ 完成 | 通过 | 自动断言确认 16 关均有敌情、15 关有 rules；虎牢关选将页展示敌情、主要威胁、推荐应对、战场规则「无额外规则」；出战后 HUD/初始资源正常 |
| R25-4 | 人物 UI 形象升级 | node --check + scripts/verify-r25.js；浏览器截图目检 | ✅ 完成 | 通过 | 22 将数据完整；5 将 portrait 覆盖路径有效；无图武将使用 hero-fallback 头像兜底；武将府/选将详情布局无主要溢出或重叠 |
| R26-1 | 赵云真实素材接入试点 | node --check + scripts/verify-zhaoyun-assets.js + PNG 透明采样 | ✅ 完成 | 通过 | `portrait/sheet/icons/passive-icon` 均为 RGBA PNG；角落透明采样通过；赵云 `stripBg:false` 直接绘制透明 sheet，避免 `file://` canvas 清理失败回退程序化；详情页被动图标与说明同卡展示；武将府排序/筛选控件存在。注：赵云需在出战阵容中，并在战斗内招贤抽到后部署，才会出现在战场 |
| R27-1 | 战场视觉与武将可读性优化 | node --check + scripts/verify-r27-battlefield.js + 浏览器尺寸实测 | ✅ 完成 | 通过 | 战场改为草地/土路/远山/关门绘制；赵云战斗尺寸提升至 76；塔绘制按 y 排序，名牌加底并做避让；`#game` 使用 `aspect-ratio: 960 / 600`，浏览器实测显示 567×354，比例 1.6，与 canvas 内部 960×600 一致，无拉伸；无 console 报错 |
| R28-1 | 战斗主界面 UI/UE 重构 | 待测 | ⬜ 待办 | 待测 | 需验证战斗 UI 层级、画布比例、右侧面板响应式与 console |
| R28-2 | 战场画布防竖向拉伸修正 | node --check + scripts/verify-r28-ui.js + 回归脚本 | ✅ 完成 | 通过 | 自动检查确认 `#stage` 使用独立双列网格、`#game` 固定 `aspect-ratio: 960 / 600` 且 `height:auto`、右侧面板 `max-height + overflow-y:auto`；当前会话本地服务启动被系统结束、DevTools 实例被占用，未完成浏览器截图实测 |
| R29-1 | 战前流程 UI/UE 统一 | node --check + scripts/verify-r29-prebattle-ui.js + 全量回归脚本 | ✅ 完成 | 通过 | 自动检查确认选关、选将、武将府共享军府面板视觉，选将左右布局与武将府工具条/详情档案样式存在；移动端单列兜底存在。浏览器截图实测仍受当前会话本地服务/DevTools 限制未完成 |
| R30-1 | 主界面与反馈 UI polish | node --check + scripts/verify-r30-polish.js + 全量回归脚本 | ✅ 完成 | 通过 | 自动检查确认主界面底部控制面板、按钮 hover/active/focus、HUD toast、战报弹窗与移动端兜底样式存在；浏览器截图实测仍待可用本地服务/DevTools 环境补测 |
| R31-1 | 战斗操作状态卡 | node --check + scripts/verify-r31-battle-state-ui.js + 全量回归脚本 | ✅ 完成 | 通过 | 自动检查确认待部署/已选/空状态卡、候选 deploy/upgrade/short 状态、数值格与操作提示样式存在；浏览器目检仍待可用环境补测 |
| R32-1 | 全局视觉一致性收尾 | node --check + scripts/verify-r32-global-polish.js + 全量回归脚本 | ✅ 完成 | 通过 | 自动检查确认羁绊需求说明移除内联样式、overlay 动画规则无重复、滚动条样式存在、旧式 tower/train/intel/chip 控件统一；浏览器目检仍待可用环境补测 |
| R33-1 | 战斗 HUD 信息架构重排 | node --check + scripts/verify-r33-hud-layout.js + 全量回归脚本 | ✅ 完成 | 通过 | 自动检查确认 HUD 已分为关卡/资源/技能/控制组，原按钮和数值 id 保留，移动端分组换行样式存在；浏览器目检仍待可用环境补测 |
| R34-1 | 状态色彩语义增强 | node --check + scripts/verify-r34-state-colors.js + 全量回归脚本 | ✅ 完成 | 通过 | 自动检查确认 HUD 资源 data-kind、状态色 token、火计/空城计语义色、候选 deploy/upgrade/short、关卡 cleared/boss/locked 状态色存在；浏览器目检仍待可用环境补测 |
| R35-1 | UI 基础质量门禁 | 待测 | ⬜ 待办 | 待测 | 需验证缓存版本一致、CSS 基础结构、粗糙样式防回退、全量回归 |
| R36-1 | 战场画框比例锁定 | 待测 | ⬜ 待办 | 待测 | 需验证固定比例外框、canvas 铺满外框、侧栏独立滚动 |
| R37-1 | UI/UE 美术资产包规划 | 文档审阅 | ✅ 完成 | 通过 | 已建立 `docs/ui-art-asset-brief.md`，P0-01~P0-14 均为直接复制提示词；同时覆盖素材清单、尺寸、透明 PNG、分格、命名、优先级与验收标准 |
| R38-1 | P0 UIUE 素材处理与接入 | node --check + scripts/verify-r38-uiue-assets.js + 全量回归脚本 | ✅ 完成 | 通过 | 自动检查确认 `素材/UIUE` 14 个源文件存在，运行时 UIUE 角色/敌军/图标为 RGBA PNG，虎牢关背景为 960×600，赵云/五类敌军/战场/HUD 图标路径均已接入；当前会话本地 http 服务被系统结束、DevTools MCP profile 被占用，浏览器截图目检待可用环境补测 |
| R39-1 | 战场路线融合与敌兵落地修正 | node --check + scripts/verify-r39-battlefield-integration.js + 全量回归脚本 | ✅ 完成 | 通过 | 自动检查确认 UIUE 背景下使用低透明 `drawRouteGuide()` 替代旧厚路面，敌兵按 `sheetFoot/drawY` 脚底锚点绘制，并启用精灵表行走帧切换；缓存版本升级到 `20260614-r39-battlefield-integration`；浏览器截图目检待可用环境补测 |
| R40-1 | 虎牢关路线锚点与行走融合 | node --check + scripts/verify-r40-path-anchors.js + 全量回归脚本；浏览器尝试 | ✅ 完成 | 通过 | 自动检查确认虎牢关使用专用视觉路径、路径附近格子阻挡、低透明尘土+足迹提示、敌兵脚步抬升/阴影脉动、UIUE 背景不叠旧城门；缓存版本升级到 `20260614-r40-path-anchors`。浏览器实测受限：`python3 -m http.server` 被系统结束，本地端口监听返回 EPERM，DevTools MCP profile 被占用 |
| R41-1 | 战报结算 UI 升级 | node --check + scripts/verify-r41-result-report.js + 全量回归脚本；浏览器尝试 | ✅ 完成 | 通过 | 自动检查确认结算卡包含战报徽记、摘要格、奖励芯片、下一步提示、胜负状态样式，`win/lose` 使用结构化 payload 且移除旧 `shardLine` 文案；缓存版本升级到 `20260614-r41-result-report`。浏览器实测受限：本地端口监听不可用，DevTools MCP profile 被占用；Playwright 包存在但浏览器二进制未安装 |
| R42-1 | 主界面战役司令台升级 | node --check + scripts/verify-r42-home-command.js + 全量回归脚本；浏览器尝试 | ✅ 完成 | 通过 | 自动检查确认主界面包含司令台 DOM、战役档案渲染、进度条、武将/关卡/掉落指标、继续/武将府入口、桌面双栏和移动端单列样式；缓存版本升级到 `20260614-r42-home-command`。浏览器实测仍受当前环境限制：本地端口监听不可用，DevTools MCP profile 被占用，Playwright 浏览器二进制未安装 |
| R43-1 | 关卡选择战役沙盘升级 | node --check + scripts/verify-r43-campaign-map.js + 全量回归脚本；浏览器尝试 | ✅ 完成 | 通过 | 自动检查确认选关页使用战役卡片网格，展示关卡编号、状态、Boss/常规标记、敌情标签、威胁/应对摘要、掉落提升武将；未解锁点击逻辑保持禁用，移动端单列兜底；缓存版本升级到 `20260614-r43-campaign-map`。浏览器实测仍受当前环境限制：本地端口监听不可用，DevTools MCP profile 被占用，Playwright 浏览器二进制未安装 |
| R44-1 | 战场路线与士兵行走融合修正 | node --check + scripts/verify-r44-battlefield-grounding.js + 全量回归脚本；浏览器尝试 | ✅ 完成 | 通过 | 自动检查确认 UIUE 虎牢关分支不再绘制旧厚路线，改用分段尘线/边缘磨痕/小脚印；敌兵绘制包含脚底接触、倾斜、落脚尘土和四帧行走循环；缓存版本升级到 `20260614-r44-battlefield-grounding`。浏览器实测受限：内置浏览器安全策略拦截 `file://`，`python3 -m http.server` 被系统结束，DevTools MCP profile 被占用 |
| R45-1 | 选将出战军册升级 | node --check + scripts/verify-r45-deck-command.js + 全量回归脚本；浏览器尝试 | ✅ 完成 | 通过 | 自动检查确认选将页新增出征军册顶部、战场指标、战场军报、阵容槽位、军册/将领档案双栏、详情入阵状态和移动端兜底；原 `deckGrid/deckDetail/deckCount/deckStart/deckBack` 保留，选择/出战逻辑入口未破坏；缓存版本升级到 `20260614-r45-deck-command`。浏览器实测仍受限：DevTools MCP profile 被占用，本地服务此前被系统结束 |
| R46-1 | 武将府军籍档案升级 | node --check + scripts/verify-r46-codex-command.js + 全量回归脚本；浏览器尝试 | ✅ 完成 | 通过 | 自动检查确认武将府新增将府军籍顶部、总量/已拥有/可升级/可招募/筛选结果指标、军籍列表状态徽记、羁绊卷宗、详情页头和移动端兜底；原 `codexSort/codexFilter/codexHeroes/codexBonds/detailBody/detailBack` 保留，招募/升级逻辑入口未破坏；缓存版本升级到 `20260614-r46-codex-command`。浏览器实测仍受限：DevTools MCP profile 被占用，本地服务此前被系统结束 |
| R47-1 | 战斗军报反馈层升级 | 待测 | ⬜ 待办 | 待测 | 需验证波次军报横幅、名将登场/授首反馈和 Boss 状态血条 |
| R48-1 | QA 体验修复 | Headless Chrome + verify 全量回归 | ✅ 完成 | 通过 | 移动主界面按钮回到场景内；file:// 无 SecurityError；选将底栏 `footOverlap=false`；verify-r25~r46 通过 |
| R49-1 | 路径贴合与造塔平台 | node verify-r49 + 全量回归 | ✅ 完成 | 通过 | 对照底图重校土路锚点/11 个木塔平台；常驻金色平台标记；样条密采样 8px |
| R51-1 | 战场左侧界面修正 | node verify-r51 + 全量回归 | ✅ 完成 | 通过 | 造塔点离路；UIUE 模式去路径脚印；空闲仅小旗标；画布深棕外框 |

---

## 六、备注

- 已知工具问题：CDP 截图偶发超时，用 `format=jpeg, quality=60/75` 可绕过（与游戏无关）。
- 已知工具问题：2026-06-12 复测时 Chrome DevTools MCP 仍被 `chrome-profile` 占用锁阻塞；本轮 R25 改用本地静态服务 `http://127.0.0.1:8877` + Codex 内置浏览器完成点击实测与截图目检。
- 测试手段亮点：页内无头模拟器（自动布塔 + 快进跑波 + 统计漏怪/余粮）用于量化平衡；停帧（running=false）+ 手动 draw() 用于定格瞬时粒子特效截图。
- 美术替换说明详见 `docs/art-system.md` §2；图片放入 `assets/` 后填 `SPRITE_ASSETS[id].img` 即生效。
- 真实素材建议：后续武将图片最好直接提供透明 PNG-32（带 alpha），不要导出成带棋盘格的 RGB 截图；精灵表建议等格网格、动作不越界、脚底对齐，并提供列数/行数/每帧尺寸。
