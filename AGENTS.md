# 项目：tower-defense

基于三国题材的网页塔防游戏（原生 HTML5 Canvas + JS，无构建依赖）。

# 开发规则（强制）

## 1. 文档先行
- 所有需求和设计，必须先落到 `DESIGN.md`，再开始编码。
- 文档化内容应包含：需求描述、设计方案、涉及的文件/模块、验收标准。
- 未经文档化的需求不得直接开发。如果用户提出新需求，先写入 `DESIGN.md` 的「需求与设计」区，再动代码。

## 2. 进度跟踪（表格形式）
- 在 `DESIGN.md` 中维护「实现进度」与「测试进度」两张表格。
- 开发过程中持续更新：每完成一个功能的实现或测试，立即更新对应表格行。
- 状态图例：⬜ 待办 / 🚧 进行中 / ✅ 完成 / ❌ 失败

## 3. 测试
- 前端功能用 chrome-devtools MCP 在浏览器中实测（导航 → 操作 → 截图/读 console 验证）。
- 测试结果记录到 `DESIGN.md` 的「测试进度」表。

## 4. 文档拆分（工程变大时）
- 初期单一 `DESIGN.md` 即可。
- 当某个功能模块的设计内容过多、或模块数量增长导致 `DESIGN.md` 臃肿时，按功能拆分到 `docs/` 目录下的多个文档（如 `docs/heroes.md`、`docs/skills.md`、`docs/levels.md`）。
- 拆分后，`DESIGN.md` 退化为「总览 + 索引 + 全局进度表」，链接到各子文档；各子文档维护自己模块的详细设计，全局进度表仍集中在 `DESIGN.md`。
- 拆分时机由开发判断并在 `DESIGN.md` 记录一次。

# 工作流约定
- 实现前：在 `DESIGN.md`（或对应子文档）写需求/设计 + 在 `DESIGN.md` 进度表加 ⬜ 行。
- 实现中：把该行置 🚧。
- 实现后：置 ✅，并补「测试进度」表对应行。
